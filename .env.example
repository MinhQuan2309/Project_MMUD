JWT_SECRET=dev-secret-for-local-test
JWT_ALG=HS256
JWT_EXPIRE_SECONDS=3600

WEBHOOK_SECRET=change-this-webhook-secret
WEBHOOK_TOLERANCE_SECONDS=300

LOG_LEVEL=INFO

# Khi Quân tích hợp Vault thật, có thể bật các biến này.
# VAULT_ADDR=http://vault:8200
# VAULT_SECRET_MOUNT=secret
# VAULT_SECRET_PATH=project-mmud
